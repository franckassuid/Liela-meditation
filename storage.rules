rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Public, free media only. Premium assets require a backend-authorized delivery path.
    match /public/sessions/{path=**} { allow read: if true; allow write: if false; }
    match /{path=**} { allow read, write: if false; }
  }
}
